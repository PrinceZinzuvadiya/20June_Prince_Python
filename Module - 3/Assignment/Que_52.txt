How can you pick a random item from a list or tuple?
    You can use the random.choice() function to pick a random item from a list or tuple.

    Example:

    import random

    my_list = [1, 2, 3, 4, 5]

    random_item = random.choice(my_list)

    print(random_item)